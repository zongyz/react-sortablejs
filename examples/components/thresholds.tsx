import React from "react";
import styled from "styled-components";

/** @todo */
export function Thresholds() {
  return (
    <div>
      <P>This is on the todo list.</P>
    </div>
  );
}

const P = styled.p`
  margin-bottom: 0.5rem;
`;
